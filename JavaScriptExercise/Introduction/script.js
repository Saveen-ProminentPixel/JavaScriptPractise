const button = document.querySelector("button");

const buttonClickedHandler = () => {
    alert("You have made a mistake, now get ready to die!!");
};

button.addEventListener("click", buttonClickedHandler);

// function buttonClickedHandler() {
//     alert("You have made a mistake, now get ready to die!!");
// }

